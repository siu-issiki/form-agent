# 受付・エラー文の限定追加

> 個別実装時点の記録です。統合時点の状態は[信頼性改善の検証記録](reliability-improvements-2026-09.md)を参照してください。

基準 `bb8a6ff`。確定 uncertain 全件分析のうち、可読文を転記して未検出を再現した新受付 6 件と明示エラー 9 件を対象にした。一般ジョブの再送・再分類や実サイトへの操作は行っていない。当時の完全 DOM は保存されていないため、保存画像の文言から作った管理下再現であり、全実先の原因確定ではない。

## 製品変更

`SUBMISSION_CONFIRMATION_PATTERN` に次を行頭・文末境界付きで追加した。

- row 132: 「お問い合わせ完了」＋「お問い合わせ受付完了」の二見出し
- row 173: 問い合わせのお礼＋内容確認後の連絡
- row 176: 「お問い合わせが送信されました。」
- row 304: 問い合わせのお礼＋担当からの返信
- row 355: 完了見出し＋「お問い合わせ内容を送信いたしました。」
- row 638: お問合せのお礼＋担当者から2～3日以内の連絡

単独のお礼・完了見出しだけには広げない。既存の新規表示・要求観測・frame の関連付け・確認段階・FAQ例示フィルタは変更しない。

`SUBMISSION_FAILURE_PATTERN` に、row 161/170/171/175/185/213/367/614/653 の Not Found 複合文、スパム防止チェック、入力内容エラー、スパム疑い、認証失敗を追加した。カタカナ指定と入力内容エラーが続く例では複合文として抽出し、直前の「表示例」から始まる FAQ を途中行だけ拾わない。エラー理由を詳細化する変更であり、受信成功とせず、表示文の「再度お試しください」に従って再送することもない。

## 赤→緑

1. 追加テストだけを旧製品で実行: 34 pass / 15 fail。新受付6・エラー9の positive が全て失敗した。ログ `artifacts/outcome-coverage-20260905/old-red.log`。
2. パターンの限定追加後: 対象49 pass / 0 fail。新規受付、before既存同文、要求未観測、確認前、明示未送信、FAQ同一行/複数行、疑問・否定、単独お礼、エラー例示と再試行指示だけの negative を検証。ログ `new-green.log`。
3. 全チェック: typecheck / lint 成功、unit 804 pass、Worker 248 pass。既存の frame/許可済み要求/送信後再操作防止等の回帰テストも含む。ログ `typecheck.log` / `lint.log` / `test.log`。

受信のない/無関係 frameの表示は既存 driver の境界を利用する。新パターンだけの unit 成功を実browser送信成功と報告しない。専用 test-system worktree `test-system-outcome-coverage-20260905` に受付6・エラー代表3・未修正境界1の10シナリオを追加した。製品旧版に対する本番管理下 red / 新版 green と証跡 SHA 検証は root のリリース工程で行う。

## row 371 は安全境界を維持

新規の既知受付文があっても、resetされた form と常設確認案内が残る場合、現在は確認前と区別できず未確認になることを独立テストで固定した。管理下 `ajax-audit-reset-static-guidance` もその結果を期待する。

所属 form と結果要素・新規表示の関係を特定できる当時 DOM がなく、単に残存 form を許す例外追加は別 form の受付や確認前を成功と誤認し得る。今回は広い pending veto 解除を行わず、将来必要な証拠/限定設計を残課題とする。画像の受付らしさだけで実ジョブを sent に変更しない。

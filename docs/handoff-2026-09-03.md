# Form Agent 残タスク・ハンドオフ（2026-09-03）

- 作成日: 2026-09-03
- 対象: `siu-issiki/form-agent` / `siu-issiki/form-agent-test-system`
- 前提: 2026-09-03 夕方に Cloudflare Workers を Paid、BrowserUse を dev（有料、同時 session 10）プランへ移行済み。`max_concurrency` は 10（PR #65）
- 再開時の推奨: P0-1〜P0-6 のコード対応は実装済みであり、まず管理下 E2E で効果を確認する

## 引き継ぎ概要

2026-09-02〜03 で次を本番へ入れた。

| PR | 内容 |
| --- | --- |
| #48 | 送信前の独立レビュー（Guardian 相当）、observe 結果の非信頼マーキング、tool エラーの細分化と guidance |
| #50 | BrowserUse 接続の再試行と close 診断 |
| #51 | BrowserUse session を REST API v4 で作成し、ジョブ終了時に `stop` で明示停止。Workers Logs を有効化 |
| #52 | Workers の `Illegal invocation` 対策（fetch のラップ）、API 診断ログ |
| #53 | 並列数を 3 に変更、429 時の active session 件数記録 |
| #54 | 3 並列計測の結果を設計書へ記録 |

本文書を書き始めた 2026-09-03 午前の時点では、本番は version `6b8398a2`、`max_concurrency: 3`、`AGENT_DRY_RUN=false`、Queue 稼働中で、処理中ジョブは 0 件だった。その後の deploy と設定変更は「午後の追加作業」以降に記す。

> [!CAUTION]
> production の `AGENT_DRY_RUN` は `false` であり、通常ジョブは実送信する。外部サイトを対象にしたジョブ登録・再実行は、明示的な承認なしに行わない。送信なし検証ではジョブ payload へ `_formAgentDryRun: true` を必ず指定する。

## 計測結果（管理下テストシステム 19 シナリオ）

| 構成 | 合格 | session 停止 | 作成 API 429 | CDP 切断 1011 | exceededCpu |
| --- | --- | --- | --- | --- | --- |
| 逐次（PR #48 後） | 19 / 19 | 仕組みなし | 該当なし | 0 | 1（無言失敗、後日同型を観測） |
| 直結方式・5 並列 | 15 / 19 | 仕組みなし | 該当なし | 16 | 1 |
| 明示停止・5 並列 | 15 / 19 | 18 / 18 | 15 | 0 | 0 |
| 明示停止・3 並列 | 17 / 19 | 19 / 19 | 0 | 0 | 0 |

確定した事実:

- CDP 直結（`connect.browser-use.com`）では WebSocket を閉じても managed browser は停止せず、15 分の寿命まで同時 session 数を占有する（BrowserUse 公式ドキュメントに明記）。
- session を毎回停止しても 5 並列では作成 API が `429 Too many concurrent active sessions` を返す。この account の同時 session 上限は 5 未満で、Free プランの 3 と推定する。3 並列では 429 が出ない。
- Workers では `fetch` をオブジェクトのメソッドとして呼ぶと `TypeError: Illegal invocation` になる。fake fetcher の単体テストでは再現しない。
- `cdpUrl` は `https:` 形式で、`/json/version` から `webSocketDebuggerUrl` を取得して接続する。

## 採用済みの設計判断

- BrowserUse は 2026-09-03 夕方に dev（有料）プランへ移行済みで、同時 session 上限は 10 である。これに合わせて `max_concurrency` を 10 とした（PR #65）。10 並列の実測は未実施であり、`browser_use_session_limit` で計測して調整する。
- Cloudflare Workers は 2026-09-03 夕方に Paid プランへ移行済みである。CPU 上限は既定 30 秒で、`limits.cpu_ms` により最大 5 分まで変更できるが、現在は設定していない。Workers Logs（Paid プランの保持は 7 日）は有効。
- 送信前の独立レビューは `INPUT_MISMATCH` の deny だけ 1 回の修正を許可し、他の deny と 2 回目の deny は `uncertain` で終了する。deny 回数は D1 に永続化する。
- codex review の指摘は脅威モデルで評価し、限定的なら設計書に残存リスクとして明記して受容する（PR #48 の 4 件）。
- E2E 用トークンは `.env.e2e`（リポジトリルート、git 管理外）に置き、`bun --env-file=.env.e2e <script>` で読み込む。

## 残タスク

優先度は「実サイトへの実送信で問題になる順」とする。この一覧は BrowserUse Free（3 並列）当時に作成したものであり、並列関連の記述は 2026-09-03 夕方の dev プラン移行と `max_concurrency: 10`（PR #65）より前の状況を指す。

### P0: 3 並列運用で再現する失敗

| # | タスク | 現状 | 完了条件 | 主な確認箇所 |
| --- | --- | --- | --- | --- |
| P0-1 | 並列時の `multi-step` で turn 6 の `click` が `CDP_COMMAND_FAILED` になる | 実装済み（PR A）。`click` / `fill` / `select` 中の CDP コマンド失敗を `BrowserElementOperationError` へ変換し、モデルへ `ELEMENT_UNAVAILABLE` と再観察の guidance を返す。診断は `ELEMENT_OPERATION_CDP_FAILED` | 3 並列で 3 回実行し、`multi-step` が click 失敗後に続行して `sent` になる | `src/browser-use-cdp-driver.ts`（`clickNonSubmit`）、`src/responses-agent-executor.ts`（catch）、`test/worker.test.ts` |
| P0-2 | 禁止フォームの submit を handler がブロックした後、モデルが `finish_prohibited` ではなく `finish_failed` を選ぶ | 実装済み（PR A）。`SubmitProhibitedError` を追加し、`SUBMIT_PROHIBITED` と `prohibitedReasonCodes` / `pageProhibited` を返す。guidance と system prompt にも追記 | 3 並列で 3 回実行し、`external-iframe-secondary` が `prohibited` で終わる | `src/restricted-browser.ts`（`validateSubmit`）、`src/responses-agent-executor.ts`（`TOOL_ERROR_GUIDANCE`） |
| P0-3 | iframe 側の禁止文言に対して handler の禁止根拠検証が通らず `uncertain` になる | 実装済み（PR A）。`validateProhibited` が「観察が最新・URL 一致・code 不在」のときだけ input revision ごとに 1 回再観察する。`Page.getFrameTree` の補完は不要と判断（下記参照） | 3 並列で 3 回実行し、`FINISH_PROHIBITION_NOT_VERIFIED` が減少する。残る場合は診断とともに記録 | `src/restricted-browser.ts`（`validateProhibited`） |
| P0-4 | `submitting` 中に Worker を強制停止しても再送されないことの検証 | 経路は Workers テストで固定済み（PR A）。再配信は ack され、状態は `submitting` のまま残り、`job.redelivery_ignored` で検出できる。2026-09-03 の 3 並列計測 3 回目で実機でも確認済み（下記参照） | 完了。以後は同じ照合と人手確定の手順を runbook に従って実施する | `src/worker.ts`、`src/d1-job-store.ts`、`docs/operations.md` |
| P0-5 | 実サイトの必須 select / radio に対応する `formValues` が無く、モデルが安全側で `uncertain` にする | 実装済み（本 PR）。`formValues` の値に選択肢候補リスト（1〜10 要素、各要素 1〜256 文字、合計 2,048 文字以下）を許可し、`select` だけが受け取る。信頼済み handler が option 値・option text・ラベルとの完全一致で候補順に選び、radio では同じ群のより前の候補が一致する場合に拒否する。checkbox は `checked` / `unchecked` / ラベル一致だけを受け付ける（任意文字列での uncheck は廃止） | 管理下テストシステムの選択肢シナリオ 3 件が `sent` になり、`--choices` 付きの実サイト dry-run で該当 2 件が `DRY_RUN_COMPLETE` になる | `src/restricted-browser.ts`（`readTrustedFormValues`）、`src/browser-use-cdp-driver.ts`（`select`）、`src/worker.ts`（`POST /jobs`）、`tools/campaign-dry-run.ts`（`--choices`） |
| P0-6 | 起動時の読み込み待ち 10 秒を超えて `PAGE_NOT_READY` で失敗する | 実装済み（本 PR）。bootstrap navigate だけ readyState 待ちを 25 秒にし、`PAGE_NOT_READY` のとき driver 内部で 1 回だけ navigate をやり直す。回数は bootstrap 全体で 1 回しか増えないため dry-run の再 navigate 禁止には掛からない。接続断は待たずに throw し、abort 時は coordinator が bootstrap 中の driver を閉じる。通常の `navigate` は 10 秒・再試行なしのまま | 管理下テストシステムの `slow-initial-load` が `sent` になり、実サイト dry-run で該当 1 件が `PAGE_NOT_READY` にならない | `src/browser-use-cdp-driver.ts`（`navigate` / `#waitForReadyState`）、`src/browser-tool-handler.ts`（`#pendingDriver`） |

P0-4 の実機確認について: 2026-09-03 の 3 並列計測 3 回目で、`native-get` の Worker が `outcome: exceededCpu`（報告 `cpuTime` 172 ms、`wallTime` 55 秒）で強制終了し、ジョブは送信権取得後・activation 前の `submitting` で停止した。Queue の再配信は `job.redelivery_ignored` で ack され、自動再送は起きなかった。テストシステム側の受信 0 件と `after_submit` 証跡なしを照合したうえで、runbook の手順で人手により `uncertain`（`OPERATOR_CONFIRMED_UNCERTAIN`）へ確定した。意図した強制停止ではなく `exceededCpu` による偶発の停止だったが、P0-4 が想定した経路そのものを通っているため、実機でも確認済みとする。

P0-3 の `Page.getFrameTree` 補完について: `#frameParentIds` は初期化時の `Page.getFrameTree` に加えて `Page.frameAttached` と `Page.frameNavigated` でも登録し、エントリを削除しない。`Page.enable` は `getFrameTree` より前に呼ぶため、初期化後に増えた frame は必ず event で登録される。したがって `observe` 内での補完は不要と判断し、実装していない。

### 実サイト dry-run パイロット（5 件、2026-09-03）

`_formAgentDryRun: true` で 5 件を実行した。企業名・URL・入力値は記録しない。

| 結果 | 件数 | reason code |
| --- | --- | --- |
| `prohibited` | 2 | `DRY_RUN_COMPLETE` |
| `uncertain` | 2 | `INQUIRY_TYPE_VALUE_UNMAPPED` / `MISSING_CONTACT_METHOD_MAPPING` |
| `failed` | 1 | `PAGE_NOT_READY` |

`uncertain` の 2 件は、必須の選択肢項目に対応する `formValues` が無く、モデルが値を作らずに終了したものである。信頼境界としては正しい挙動であり、P0-5 の候補リストで対応した。`failed` の 1 件は起動時の読み込みが 10 秒以内に終わらなかったもので、P0-6 で対応した。いずれも実サイトへは送信していない。再実行による確認は未実施である。

### P1: 運用と可観測性

| # | タスク | 現状 | 完了条件 |
| --- | --- | --- | --- |
| P1-1 | Paid 移行後に `exceededCpu` が再発しないことの確認 | Free プラン時に計 3 件を単発で観測し（報告 cpuTime 165 ms / wallTime 91 秒、172 ms / 55 秒）、PR #62 deploy 以降は 30〜40% へ急増した。3 件とも `before_submit` 撮影直後から送信前レビュー呼び出しまでの区間で発生している。Paid 移行と再 deploy の後は 9 呼び出しで 0 件、6 シナリオ 6 / 6 合格。原因は Free プランの負荷時の isolate 強制終了と推定する | Paid 移行後に 23 シナリオ 2 巡を実行し、`exceededCpu` が再発しないことを確認する。再発した場合は Workers Logs（Paid プランの保持 7 日以内）で該当 invocation の console 出力を確認し、原因または再現条件を設計書へ記録 |
| P1-2 | token、処理時間、BrowserUse 待ち時間、費用の記録 | 実装済み。1 run 1 件の `agent.run_metrics` に turn、Provider 呼び出し、token、送信前レビューの allow / deny、browser 接続時間、実行時間、固定 outcome を保存する（migration なし） | 残りは deploy 後に dry-run 1 件で記録を確認し、runbook の集計クエリで 1 件原価を実測すること |
| P1-3 | 証跡スクリーンショットの保存期間と R2 孤児オブジェクトの扱い | 孤児検出は実装済み。R2 put の前に `evidence.intent` を記録し、同じ `events.id` を `evidence.captured` / `evidence.capture_failed` へ遷移させる。残存 `evidence.intent` 行の確認と削除手順は runbook に記載 | 残りは保存期間ポリシーの決定（現在は無期限、R2 ライフサイクルルールなし）と、残存行確認の運用への組み込み |
| P1-4 | Queue retry の指数 backoff と jitter | 実装済み。30 秒を基準に 2 倍へ増やし ±20% の jitter を掛けたうえで 300 秒で上限を設ける。実際の遅延は `job.retry_scheduled` の `delaySeconds` に保存する。DLQ 再登録手順は runbook に追加済み | 残りは一時障害の細分類と、20 並列での検証 |
| P1-5 | BrowserUse の残骸 session の定期確認 | 週次で `tools/browser-use-sessions.ts list` を実行し、active が 0 件でなければ `metadata.jobId` で D1 の状態を確認して停止する手順を runbook に追加済み | 残りは `BROWSER_USE_API_KEY` を持つ担当者の割り当てと、週次実行の定着 |

### P2: 機能拡張と実サイト対応（明示承認後）

| # | タスク | 備考 |
| --- | --- | --- |
| P2-1 | 運用 API（利用者別認証、ジョブ一覧・キャンセル、`submitting` / `uncertain` 照合） | 現在は単一 Bearer と `wrangler` 手動照合 |
| P2-2 | 実サイト互換性の分類（cross-origin iframe、確認画面、複数ページ、Shadow DOM、添付、CAPTCHA） | 外部実サイトへの送信は未実施。承認者と件数上限の決定が先 |
| P2-3 | 禁止判定パターンの実サイト表現への拡張と監査 | 固定パターンと独立レビューで補完中 |
| P2-4 | 送信前レビューの残存リスク 4 件（最終照合から activation までの窓、snapshot の対象外項目、修正証明の粒度、200 field 超の再探索） | PR #48 で受容済み。設計書「送信前の独立レビュー」参照 |
| P2-5 | 並列数の引き上げ | BrowserUse dev プランへ移行し `max_concurrency` を 10 にした（PR #65）。10 並列での実測が未実施であり、23 シナリオ 2 巡で計測する |
| P2-6 | 外部 form host の allowlist 運用検証、Provider abstraction、SPA 遅延描画時の探索再試行 | 前回ハンドオフから継続 |

## 午後の追加作業（2026-09-03）

### 選択肢候補（PR #62、テストシステム PR #10）

- テストシステムに 4 シナリオを追加し、管理下シナリオの合計を 23 とした（`inquiry-type-select` / `contact-method-radio` / `choices-combined` / `slow-initial-load`）。テストシステム側 main `4a976a54`、external `d3d6b981`。
- P0-5 対応前の form-agent（`c7bb45f6`）で `slow-initial-load` を実行すると、約 23 秒後に `BROWSER_TOOL_UNAVAILABLE`（readyState 待ちの 10 秒超過）で失敗することを確認した。
- form-agent PR #62（選択肢候補リスト、bootstrap navigate の待ちを 25 秒＋1 回再試行に変更、bootstrap 中の abort で driver を閉じる）を codex review に 3 巡通し、計 5 件を修正した（reviewer へ渡す状態キーワードの説明不足、`aria-labelledby` の連結形一致漏れ、disabled option の除外漏れ、110 秒という再試行時間の上限の記述漏れ、断片一致の除去）。deploy `e611344b`。
- 追加した 4 シナリオを単独実行（4 件同時）したところ 4 / 4 合格し、受信側の値が期待値（選択肢解決後の値）と一致した。

### session leak の連鎖（PR #63）

- `exceededCpu` で強制終了しても停止できなかった session のうち、再配信が attempt 上限（`JOB_ATTEMPT_LIMIT_REACHED`）で終わる経路のものは回収されず、BrowserUse Free プラン（同時 3）の枠を session 寿命まで塞ぐことを確認した。3 件 leak した状態では後続ジョブが全件 429 となり `BROWSER_TOOL_UNAVAILABLE` になった（23 シナリオ計測 1 巡が 0 / 23）。
- PR #63 で、attempt 上限に達した 2 つの経路でも自 jobId（＋ source タグ）の session を回収するようにした。deploy `f8d9e82a`。deploy 後は `browser_use_session_reclaimed` のログで `matched=1` / `stopped=1` を確認した。ただし他ジョブが残した leak は寿命まで塞がれたままであり、残存リスクとする。

### `exceededCpu` の急増（推定: Free プランの負荷時の isolate 強制終了）

- PR #62 の deploy 以降、Queue consumer の `exceededCpu` 発生率が急増した。deploy 前は 1% 程度（前日 130 呼び出し中 3 件、当日午前 96 呼び出し中 1 件）だったのに対し、deploy 後は 63 呼び出し中 18 件、dry-run 中 9 呼び出し中 4 件と 30〜40% に達した。
- 同時刻に 3 件が揃って停止するクラスターが多く、isolate 単位での kill とみられる。
- 停止した呼び出しの `cpuTime` は 57〜186 ms で、前日正常に完了した呼び出し（中央値 120〜208 ms、最大 325 ms）より小さい。CPU 使用量の絶対値では説明がつかず、コード側の回帰か Free プラン側の執行強化かは未切り分けだった。
- deploy `f8d9e82a` で 23 シナリオを 2 巡実行した結果は次のとおり。

  | 巡目 | 合格 | attempt 上限（`exceededCpu` 起因） | `BROWSER_TOOL_UNAVAILABLE` | 未完了 |
  | --- | --- | --- | --- | --- |
  | 1 巡目 | 20 / 23 | 3 | 0 | 0 |
  | 2 巡目 | 4 / 23 | 14 | 4 | 1 |

  2 巡目の未完了 1 件はランナーの 12 分上限に掛かって結果を取得できなかったものである。終端した 22 件の内訳は合格 4、attempt 上限 14、`BROWSER_TOOL_UNAVAILABLE` 4 である。

- 実サイト dry-run パイロット（5 件、選択肢候補ファイル付き、campaign `agb-shaken-2026-09-pilot-dryrun-v2`）の結果は次のとおり。企業名・URL・入力値は記録しない。

  | 結果 | 件数 | reason code |
  | --- | --- | --- |
  | preflight skip | 1 | - |
  | `prohibited` | 1 | `SALES_PROHIBITED` |
  | `failed`（attempt 上限） | 4 | `JOB_ATTEMPT_LIMIT_REACHED`（原因は `exceededCpu`） |

  候補ファイルによる選択肢解決の効果は、5 件中 4 件が attempt 上限で終わったため未確認である。

- 決定: Cloudflare Workers を Paid プランへ移行する。Free プランの CPU 上限は公称 10 ms/呼び出し（実測では数百 ms まで通ることが多いが、負荷時に isolate ごと強制終了される）で、Paid は既定 30 秒、`limits.cpu_ms` で最大 5 分まで変更できる。移行後に 23 シナリオ 2 巡と dry-run 5 件をやり直す。

#### Workers Paid / BrowserUse dev プラン移行後（2026-09-03 夕方）

- ユーザーが Cloudflare Workers を Paid に、BrowserUse を dev（有料）プランに移行した。この時点では `max_concurrency` は 3 のままであり、その後 PR #65 で 10 にした。
- 移行直後の計測（`f8d9e82a` のまま）ではまだ `exceededCpu` が 4 件出たため、同じコードを再 deploy した（`b4f35522`）。`Deployed triggers` の段階で code 10013 の unknown error が 1 回出たが、deployment 一覧では 100% 反映されていた。
- 再 deploy 後、6 シナリオ（`native-post-redirect` / `native-get` / `controlled-inputs` / `ajax-inline` / `multipart-post` / `inquiry-type-select`）が 6 / 6 合格し、9 呼び出しで `exceededCpu` は 0 件だった（ok 呼び出しの `cpuTime` 中央値 178 ms、最大 236 ms）。CPU 総量では説明できない（死んだ呼び出しの `cpuTime` は正常時より小さい）ため、原因は Free プランの負荷時の isolate 強制終了と推定する。確定ではなく、23 シナリオ 2 巡での再現確認が残っている。`docs/architecture.md` の「上限引き上げでは解消しない可能性が高い」という従来分析は、この結果で更新した。旧版へのロールバック実験は不要になった。
- 実サイト dry-run 5 件 v3（候補ファイル付き、campaign `agb-shaken-2026-09-pilot-dryrun-v3`、Paid 移行後）の結果は次のとおり。

  | 結果 | 件数 | 備考 |
  | --- | --- | --- |
  | preflight skip | 1 | - |
  | `DRY_RUN_COMPLETE` | 3 | v2 で `PAGE_NOT_READY` だった行と `INQUIRY_TYPE_VALUE_UNMAPPED` だった行を含む |
  | `SALES_PROHIBITED` | 1 | v2 と同じ行 |
  | `AGENT_TURN_LIMIT` | 1 | v2 で `MISSING_CONTACT_METHOD_MAPPING` だった行。観察 1 回のあと 16 turn 以上を tool 呼び出しで消費し submit に至らなかった。tool エラーは D1 の `agent.tool_diagnostic` イベント（`recordToolDiagnostic`）に記録されているが、本セッションでは D1 の読み取り権限が無く未確認である。PR #65 で Workers Logs にも `browser_tool_error`（tool 名と code の固定値）を出すようにした |

### 次にやること

1. `AGENT_TURN_LIMIT` の内訳確認: PR #65 で `browser_tool_error`（tool 名と code の固定値のみ）を executor から出すようにしたため、該当行を再実行してログで内訳を確認する（P1）。
2. 並列数: `max_concurrency` は PR #65 で 10 にした。10 並列で 23 シナリオを 2 巡計測し、`browser_use_session_limit` の `activeTotal` / `activeTagged` と 429 件数で実際に使える同時 session 数を確かめる（P1）。
3. 候補ファイルの見直し: `AGENT_TURN_LIMIT` の行はラベルが候補に無い可能性がある。1 の観測性を入れてから再実行する。
4. 23 シナリオ 2 巡の再計測: Paid 移行後は 1 巡目の途中で打ち切ったため、正式な回帰確認は未了である。打ち切り時点で 21 件中 17 件合格、失敗は移行直後の `exceededCpu` 3 件と multi-step の `MULTI_STEP_CONTROL_UNAVAILABLE` 1 件だった。

## 再開手順

### 1. 現在地の再確認（読み取り専用）

```bash
cd /Users/siu/ghq/github.com/siu-issiki/form-agent
git status -sb && git log -5 --oneline
./node_modules/.bin/wrangler deployments status --json
./node_modules/.bin/wrangler d1 execute form-agent --remote --command \
  "SELECT status, COUNT(*) AS count FROM jobs WHERE status IN ('pending','running','submitting') GROUP BY status;"
```

### 2. ローカル検証

```bash
bun run typecheck && bun run lint && bun run test:unit && bun run test:workers
./node_modules/.bin/wrangler deploy --dry-run
```

現時点の基準は unit 290 件、Workers 118 件である(PR #65 時点)。

### 3. 管理下テストシステムでの検証

`.env.e2e` に `TEST_SYSTEM_API_TOKEN` と `FORM_AGENT_JOB_API_TOKEN` がある。Worker ログは `wrangler tail form-agent --format json` で取得し、`browser_use_session_created` / `browser_use_session_stopped` / `browser_use_connect_retry` / `browser_use_api_error` / `agent_execution_error` を確認する。

```bash
# 送信なし 1 件
bun --env-file=.env.e2e run tools/test-system-dry-run.ts native-post-redirect
# 実送信 1 シナリオ（逐次）
bun --env-file=.env.e2e /Users/siu/ghq/github.com/siu-issiki/form-agent-test-system/scripts/run-production-suite.ts native-post-redirect
```

引数なしの `run-production-suite.ts` は 23 シナリオへ順次実送信する。

### 4. deploy

`docs/operations.md` の手順に従う。Queue を pause し、処理中ジョブ 0 件を確認してから `wrangler deploy` し、active version が 1 つ・100% で `AGENT_DRY_RUN ("false")` であることを確認して resume する。

## 2026-09-03 夜〜09-04 未明の追加作業

### 本番投入した PR

| PR | 内容 |
| --- | --- |
| #67 | 営業禁止判定を強化（マージ・deploy 済み） |
| #68 | 検証サービス（reCAPTCHA / hCaptcha / Turnstile）の固定許可リスト |
| #69 | 許可リスト host のサブフレーム Document を許可 |
| #70 / #72 | ブラウザ操作エラーへ CDP のメソッド・種別・コードを付与 |
| #71 | 第三者フレーム内のフォームを観察対象から除外 |
| #73 | OOPIF セッション消失の原因を確定し修正（マージ・deploy 済み） |
| #74 | 実送信経路の導入（マージ・deploy 済み、migration 0007 適用済み） |

- **#67**: 営業禁止の検出範囲を、フォーム近傍とページ全文の和集合に広げた。正規表現による判定に加え、**引用検証**を導入した。ページ側の固定パターン検出（`prohibitedReasonCodes`）に該当コードがあれば従来どおり受理し、無い場合はモデルが `finish_prohibited` に禁止文言の `evidence`（引用）を添え、handler は正規化した引用がページ本文に実在し、かつ否定形の拒否語と主語語の両方を含む場合にのみ受理する（`NO_FORM_PRESENT` は引用を使わない）。許諾を示す文言は受理しない。引用そのものは実行結果・D1・ログのいずれにも保存しない。一方、`reason`（モデルの自由記述）は従来どおり D1 に残る。あわせて `MAX_TURNS` を 16 から 40 へ引き上げ、上限到達を示す `agent_turn_limit_reached` ログを追加した。codex review を 3 巡実施し、7 件を修正した。
- **#68**: 検証サービス向けの固定許可リストを追加した。対象は https のみ、メソッドは GET/POST のみ、google/gstatic/recaptcha.net 系ドメインは `/recaptcha/` 配下のパスのみを許可する。送信内容の一致確認（claim）の対象外として扱う。脅威モデル: 許可リストの受け手は既知の検証プロバイダに限られるため、任意ホストへの送信経路にはならないと評価した。
- **#69**: 許可リストに載った host のサブフレームの Document 読み込みを許可した。OOPIF（Out-Of-Process iframe）は制限付きで動作を再開する。
- **#70 / #72**: `browser_setup_failed` のログに CDP のメソッド名・種別・数値コードを、`agent_execution_error` のログにメソッド名・種別を追加した。種別には `CONTEXT_NOT_FOUND` / `CONTEXT_DESTROYED` / `FRAME_NOT_FOUND` / `TARGET_NAVIGATED` / `NOT_ALLOWED` / `INTERNAL_ERROR` / `INVALID_PARAMS` / `METHOD_NOT_FOUND` を追加した。
- **#71**: 第三者フレーム内のフォームを観察対象から除外する処理を追加した（`skippedThirdPartyForms`）。事後の原因調査で、これは今回の障害の原因ではなかったことが判明した（該当ジョブでの除外件数は 0 件）。
- **#73**: 原因を確定し修正した。reCAPTCHA の OOPIF target に対して `Target.closeTarget` を呼ぶと所有ページごと閉じてしまい、メインセッションが `-32001`（Session not found）で失われていた。修正後は iframe target に対して `closeTarget` を呼ばず、必須の制限（`Fetch.enable` / `Target.setAutoAttach` / `addScriptToEvaluateOnNewDocument`）がすべて適用できた場合のみ動作を再開する。適用できない場合は `Page.navigate(about:blank)` を行い、（受理された場合のみ）`runIfWaitingForDebugger` を呼んでから detach して停止する。許可リストによる判定は pause の有無と独立に行う。停止済み session へのリクエストは failRequest とする。codex review を 3 巡実施し、重大度 High 5 件・Medium 1 件・Low 1 件を修正した。
- **#74**: 実送信経路を追加した。Worker 側のガードとして、承認記録（`_formAgentSendApproval`）の必須化、dry-run ジョブとの内容フィンガープリント一致確認（不一致は `DRY_RUN_CONTENT_MISMATCH`）、日次上限 `REAL_SEND_DAILY_CAP`（未設定時は 0 として全件拒否、`wrangler deploy --var` で設定）を実装した。前日以前に登録された pending な実送信ジョブの再登録は `REAL_SEND_STALE` として拒否する。実行スクリプト `tools/campaign-send.ts` は承認ファイルと `--confirm-real-send` フラグを必須とし、`--max-sends` の既定値は 5 件である。承認記録はモデルへの入力からは除外される。codex review を 2 巡実施し、重大 1 件・中 3 件を修正した。残る Low 1 件は残存リスクとして受容した（日次上限が閉じている間は stale なジョブが 429 で返る挙動）。現時点で本番の日次上限は 0 のままである。

### 実サイト dry-run の計測

同じ 50 件（offset 195）を対象に、#69・#73 の修正前後で計測した。

| 指標 | 修正前 | 修正後 |
| --- | --- | --- |
| `BROWSER_TOOL_UNAVAILABLE` | 9〜11 件 | 0 件 |
| `DRY_RUN_COMPLETE` | 11〜13 件 | 15 件 |
| CAPTCHA での接続不可 | あり | 0 件 |

累計 340 件（v4〜v12）の dry-run で実送信は 0 件である。

### 残課題

- OOPIF に対する許可リスト判定が attach 時点の初期 URL（空）で行われるため、`NOT_ALLOWLISTED` として停止してしまうケースが 50 件中 20 回発生している。reCAPTCHA v3 や非対話型 Turnstile はまだ通らない。最初の遷移後の URL で判定するよう改善が必要である。
- `FORM_PURPOSE_MISMATCH`（50 件中 8 件）が uncertain の中で最多である。
- `PROHIBITION_UNVERIFIED` が 3 件あり、「断固とした対応」「法的措置」系の語彙が原因と見られる。いずれも検索フォームのみのサイト向けのコードである。
- 送信内容の点検で、CSV 本文に含まれる候補日程が過去日になっていることが判明した。日程を修正した CSV で該当行の dry-run を取り直し、その dry-run に紐付けて承認する運用が必要である（内容フィンガープリントの仕組み上、古い dry-run では承認が通らない）。

### 次にやること

1. 日程修正後の CSV で dry-run を再実行し、承認ファイルを作成する。`REAL_SEND_DAILY_CAP: 5` で deploy し、`campaign-send` で 5 件を実送信して証跡を確認したのち、日次上限を 0 に戻す。
2. OOPIF の許可リスト判定を、attach 時点ではなく最初の遷移後の URL で行うよう改善する。
3. 用途不一致・禁止語彙の検出を拡張する。
4. 管理下 23 シナリオの回帰確認を行う（PR #68〜#74 反映後は未実施）。

## 2026-09-04 日中の追加作業（circus キャンペーンと実送信 v3）

### 本番投入した PR

| PR | 内容 |
| --- | --- |
| #76 | CSV の簡易形式（問い合わせリンク / 件名 / 本文）と登録情報のラベル別名に対応 |
| #78 | dry-run で送信直前の画面（`dry_run_before_submit`）と各欄の値（`dry_run_field_map`）を証跡として保存 |
| #79 | 証跡スクリーンショットをページ全体の縮小画像（JPEG、最大 1280×4000）にする |
| #80 | 送信リクエストの照合を緩和し、2 段階送信（確認画面 → 送信）に対応 |
| #81 | 管理下テストシステムのドメインを実送信ガードから免除（`REAL_SEND_GUARD_EXEMPT_DOMAINS`） |
| #82 | 確認画面の文言（「入力内容の確認」「この内容で送信」等）を送信完了と誤判定しないようにする |

#77（簡易 CSV で件名が空の行を適格にする）は、件名なしの CSV が現時点で無く #84 / #85 と衝突するため、ユーザー判断でクローズした（2026-09-04）。必要になれば main から作り直す。

- **#80**: 実送信 v1 で 4 件とも 0 送信になった原因は、送信 request をフォームの action と厳密に照合していたことだった。ユーザー判断で照合を外し、レビュー許可後は同一ドメインへの非 safe request を上限 5 本（`SUBMIT_REQUEST_LIMIT_REACHED`）まで通す。1 回目の活性化で完了を確認できないときは `submit_stage_pending` を返し、最大 3 段階（`MAX_SUBMIT_STAGES`）まで活性化できる。2 段階目以降はレビュー済みの値（メールアドレスと本文先頭 40 文字）がページに映っていることを handler が確認する（`SUBMIT_STAGE_UNVERIFIED`）。二重送信は許容する、がユーザーの決定である。
- **#82**: 管理下 `confirm-page` シナリオで確認画面を完了と誤判定していたため、`SUBMISSION_PENDING_PATTERN` に一致するページは完了として読まない。

### 実送信 v3 の結果（campaign `circus-2026-09-send-v3`、承認 4 件）

承認は CSV の行 2 / 12 / 13 / 16 の 4 件（dry-run v4 の証跡を全体画像で点検したうえで承認）。`REAL_SEND_DAILY_CAP: 8` で deploy して実行し、終了後に var なしで deploy して上限を 0 に戻した（version `58f0e3c0`）。

| 行 | 結果 | 送信 request | 送信後の画面 | 判断 |
| --- | --- | --- | --- | --- |
| 2 | `AGENT_RESULT_UNKNOWN` | 2 段階とも観測 | 確認画面 → 「送信する」 → フォーム先頭に戻る | 到達の可能性が高いが未確認 |
| 12 | `AGENT_RESULT_UNKNOWN` | 観測 | フォームが空にリセット、完了文言なし | 到達の可能性が高いが未確認 |
| 13 | `SUBMIT_RESULT_UNKNOWN` | 観測 | Contact Form 7 の「メッセージの送信に失敗しました」 | 未達 |
| 16 | `AGENT_RESULT_CONFLICT` | 観測 | フォームが空にリセット、完了文言なし | 到達の可能性が高いが未確認 |

確定 sent は 0 件である。到達の確認は送信元アドレス宛の自動返信メールでしかできない。**二重送信になるため、この 4 件は再送しない。**

失敗の内訳は次のとおり。行 12 / 16 は request 観測後に `submit_stage_pending` が返り、モデルが送信後の大きなページを `observe` し続けた末に `PROVIDER_RESPONSE_INVALID`（行 2 も同様）と `AGENT_CONTEXT_TOO_LARGE`（request body 128 KiB 超、入力 token は約 7,000）で終わった。行 13 は送信後のページ本文の読み取りが CDP エラーになった（`SUBMIT_READ_AFTER_TEXT`）。

対策は PR #83（マージ・deploy 済み）: 活性化直後のページがレビュー済みの値をまだ映しているときだけ `submit_stage_pending` を返し、映していなければ即座に `SUBMIT_CONFIRMATION_NOT_OBSERVED` で `uncertain` を確定する。あわせて `PROVIDER_RESPONSE_INVALID` に失敗箇所の固定 `detail` を付け、request body の上限を 512 KiB に引き上げる。

### 午後の追加 PR（すべてマージ・deploy 済み、本番 version `706510f0`、日次上限 0）

| PR | 内容 |
| --- | --- |
| #83 | 送信段階の保留判定をレビュー済み値の再掲時に限定。`PROVIDER_RESPONSE_INVALID` に固定 `detail`。request body 上限 512 KiB |
| #84 | 簡易 CSV のリンク列名「問い合わせフォームリンク」と登録ラベル「サービスページ」の別名 |
| #85 | 簡易 CSV の `http://` リンクを `https://` に置き換え、疎通はリダイレクト事前確認に委ねる |
| #86 | 送信確認の読み取りで、遷移中の一時的な CDP エラー（7 種）を確認待ちの期限まで再試行。body ノード単位で古いノードを非一致扱い |

- **#86 の経緯**: 管理下 25 シナリオの回帰で 13 件目の `external-iframe` が、送信は受理されたのに `SUBMIT_RESULT_UNKNOWN`（`SUBMIT_READ_AFTER_TEXT/CDP_COMMAND_FAILED`）で失敗した。iframe の POST による遷移中に本文ノードの読み取りが失敗し、即座に諦めていた。実送信 v3 の行 13 と同じ型である。
- **回帰結果（#86 反映後）**: 25 シナリオすべて通過（12 件 + `external-iframe` 単独 1 件 + 12 件）。`external-iframe` は #86 直後の 1 回、BrowserUse への接続に 64 秒かかった末の `CDP_COMMAND_TIMEOUT`（`BROWSER_TOOL_UNAVAILABLE`、送信 0 件）で落ちたが、単独再実行では通過した。クラウドブラウザ側の一時的な遅延と判断している。runner は最初の失敗で止まるため、残りは ID 指定で再実行した（`scripts/run-production-suite.ts <ids>`）。

### 次のキャンペーン候補（2026-09-04 午後受領、当日は送らない）

- CSV は 1,013 行（件名 1 種、本文は宛先ごとに個別、`http://` リンクが 321 行）、登録 JSON は 24 項目で必須キーはすべて揃う。#84 / #85 の反映後、ツールの読み込みで 1,013 行すべてが登録可能になった（`http://` は https に置き換え、応答しないホストは事前確認で `REDIRECT_PREFLIGHT_FAILED`）。
- 本文に申し込み URL と他社役員名が含まれる。送付内容の妥当性は送信前の証跡点検で改めて確認する。
- circus キャンペーンの残り 11 行のうち、少しの対応で候補になりうるのは 3 行: 同意ラジオ「同意する」（`privacyConsent` の既定候補に追加）、英語フォームの必須選択（英語候補の追加）、送信前レビュー `INPUT_MISMATCH`（種別ラジオが営業目的に合わない）。本文が空の 3 行は本文待ち。CAPTCHA 1 行、用途不一致 1 行、フォーム無し 3 行は対象外。

### 次にやること

1. 次キャンペーン CSV の先頭 50 件を dry-run し、通過率と uncertain の内訳を見てから残りを 50 件ずつ回す（20 並列で全体 2 時間前後）。
2. 証跡点検のうえ承認バッチを作り、実送信を再開する。手順は「dry-run → 証跡点検 → 承認ファイル → `--var REAL_SEND_DAILY_CAP:N` で deploy → `campaign-send` → var なしで deploy」。実送信 v3 の 4 件は二重送信になるため再送しない。
3. 既定候補に「同意する」系のラジオ値と英語の選択肢を追加する。CF7 の「送信に失敗しました」表示を `failed` として読む検出を追加する。
4. 残課題（OOPIF の許可リスト判定、`FORM_PURPOSE_MISMATCH`、拒否語彙）は前節のとおり。
